运算格式：

- **`$((表达式))`**
- **`$[表达式]`**

```shell
[liren@VM-8-7-centos ~]$ var=10
[liren@VM-8-7-centos ~]$ echo $[var + 20]
30
[liren@VM-8-7-centos ~]$ 
```

​	两种形式是等价的，其中表达式部分就是运算式！最后可以用变量来接收返回值，如果不接收的话，可以省略 **`$`** 符号！

​	要注意的是 **`shell`** 编程**==只支持整数运算==**！且表达式中的变量是不需要加 **`$`** 符号来表示的！



​	